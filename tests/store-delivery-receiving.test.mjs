import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('store receipts reconcile shipment quantities without changing warehouse inventory',async()=>{const m=await read('database/migrations/047_store_delivery_receiving.sql');assert.match(m,/accepted_quantity\+short_quantity\+damaged_quantity=expected_quantity/);const api=await read('app/api/store-deliveries/[shipmentId]/receive/route.ts');assert.match(api,/store_user_assignments/);assert.match(api,/sh\.status='dispatched'/);assert.doesNotMatch(api,/inventory_ledger/);assert.match(api,/store_delivery_received/)});
