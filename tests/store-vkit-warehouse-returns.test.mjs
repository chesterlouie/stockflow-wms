import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('approved VKIT returns route to the configured warehouse',async()=>{const migration=await read('database/migrations/064_store_vkit_warehouse_receiving.sql'),runner=await read('scripts/migrate.mjs');assert.match(migration,/route_approved_store_vkit_return/);assert.match(migration,/default_warehouse_id/);assert.match(migration,/awaiting_receipt/);assert.match(migration,/source_store_transaction_id/);assert.match(runner,/064_store_vkit_warehouse_receiving/)});
test('warehouse receipt creates component returns in quarantine exactly once',async()=>{const route=await read('app/api/store-transactions/vkit-return/receive/route.ts');assert.match(route,/assertWarehouseAccess/);assert.match(route,/warehouse_return_status='awaiting_receipt'/);assert.match(route,/type='hold'/);assert.match(route,/inventory_returns/);assert.match(route,/source_store_transaction_line_id/);assert.match(route,/customer_return_receipt/);assert.match(route,/warehouse_return_status='received'/)});
test('warehouse has an actionable VKIT return receipt queue',async()=>{const page=await read('app/app/returns/store-vkits/page.tsx'),runtime=await read('app/components/MobileRuntime.tsx');assert.match(page,/Awaiting warehouse receipt/);assert.match(page,/Receive components/);assert.match(page,/user_warehouse_assignments/);assert.match(page,/quarantineLocationId/);assert.match(runtime,/Store VKIT returns/)});
