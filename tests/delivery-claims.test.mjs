import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('migration 082 defines claim lines evidence events KPI SLA and RLS',async()=>{const x=await read('database/migrations/082_delivery_claims.sql');for(const t of['delivery_claims','delivery_claim_lines','delivery_claim_evidence','delivery_claim_events','delivery_claim_sla_days','delivery_claim_kpis','ENABLE ROW LEVEL SECURITY','FORCE ROW LEVEL SECURITY'])assert.match(x,new RegExp(t))});
test('claim creation is scoped numbered evidence-backed notified and audited',async()=>{const x=await read('app/api/delivery-claims/route.ts');for(const t of['assertWarehouseAccess','document_sequences','pg_advisory_xact_lock','CLM-','sales_order_lines','delivery_claim_evidence','pending_approval','warehouse_notifications','audit_logs'])assert.match(x,new RegExp(t))});
test('claim decisions enforce authority transitions settlement evidence and Store notice',async()=>{const x=await read('app/api/delivery-claims/[id]/action/route.ts');for(const t of["['owner','admin','manager']",'allowed','FOR UPDATE','assertWarehouseAccess','Settlement evidence required','delivery_claim_events','store_notifications','delivery_claim_status_changed'])assert.match(x,new RegExp(t.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))});
test('claim dashboard export navigation and documents are complete',async()=>{const x=(await read('app/app/delivery-claims/page.tsx'))+(await read('app/api/delivery-claims/export/route.ts'))+(await read('app/app/layout.tsx'))+(await read('proxy.ts'))+(await read('app/app/help/module-eight.tsx'))+(await read('app/store-portal/knowledge/page.tsx'))+(await read('docs/WAREVANTA-TECHNICAL-HANDBOOK.html'));for(const t of['Delivery claims','delivery-claims','text/csv','Release 082','082_delivery_claims'])assert.match(x,new RegExp(t,'i'))});
