import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('migration 086 defines remediation cases events KPIs and tenant controls',async()=>{const x=await read('database/migrations/086_carrier_sla_remediation.sql');for(const t of['carrier_sla_remediation_cases','carrier_sla_remediation_events','carrier_sla_remediation_kpis','investigating','awaiting_carrier','resolution_effectiveness','ENABLE ROW LEVEL SECURITY'])assert.match(x,new RegExp(t,'i'))});
test('remediation mutations enforce scope evidence lifecycle recurrence and independent verification',async()=>{const x=(await read('app/api/carrier-remediations/route.ts'))+(await read('app/api/carrier-remediations/[id]/action/route.ts'));for(const t of['assertWarehouseAccess','FOR UPDATE','Evidence required','independent_verifier','recurrence_count','carrier_sla_remediation_created','carrier_sla_remediation_transitioned','carrier_sla_notifications'])assert.match(x,new RegExp(t,'i'))});
test('remediation dashboard exposes queue KPIs timeline filters export and navigation',async()=>{const x=(await read('app/app/carrier-remediations/page.tsx'))+(await read('app/api/carrier-remediations/export/route.ts'))+(await read('app/app/layout.tsx'))+(await read('proxy.ts'));for(const t of['Carrier SLA remediation','Remediation queue','Timeline','Effectiveness','Export CSV','carrier-remediations'])assert.match(x,new RegExp(t,'i'))});
test('release 086 is documented in both knowledge bases and handbook',async()=>{const x=(await read('scripts/migrate.mjs'))+(await read('app/app/help/module-eight.tsx'))+(await read('app/store-portal/knowledge/page.tsx'))+(await read('docs/WAREVANTA-TECHNICAL-HANDBOOK.html'));for(const t of['086_carrier_sla_remediation','Carrier SLA remediation','Carrier remediation','Release 086','<td>086</td>'])assert.match(x,new RegExp(t,'i'))});
