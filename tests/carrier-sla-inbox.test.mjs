import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('migration 085 adds actionable notification metadata and unread summary',async()=>{const x=await read('database/migrations/085_carrier_sla_inbox.sql');for(const t of['entity_type','entity_id','priority','carrier_sla_inbox_summary','unread_count','GRANT SELECT'])assert.match(x,new RegExp(t,'i'))});
test('SLA inbox provides badge read controls and direct action links',async()=>{const x=(await read('app/app/layout.tsx'))+(await read('app/app/carrier-scorecards/inbox/page.tsx'))+(await read('app/api/carrier-scorecards/notifications/read/route.ts'))+(await read('scripts/report-worker.mjs'));for(const t of['slaUnread','Carrier SLA inbox','Mark all read','read_at','carrier_performance_review','carrier_contract','carrier_corrective_action'])assert.match(x,new RegExp(t,'i'))});
test('contracts and corrective actions have controlled audited actions',async()=>{const x=(await read('app/api/carrier-contracts/[id]/action/route.ts'))+(await read('app/app/carrier-scorecards/contracts/[id]/page.tsx'))+(await read('app/api/carrier-scorecards/actions/[id]/assign/route.ts'))+(await read('app/app/carrier-scorecards/page.tsx'));for(const t of['Renew contract','terminate','archive','FOR UPDATE','assertWarehouseAccess','carrier_corrective_action_assigned','audit_logs','Reassign'])assert.match(x,new RegExp(t,'i'))});
test('release 085 is discoverable in migration runner and documentation',async()=>{const x=(await read('scripts/migrate.mjs'))+(await read('app/app/help/module-eight.tsx'))+(await read('app/store-portal/knowledge/page.tsx'))+(await read('docs/WAREVANTA-TECHNICAL-HANDBOOK.html'));for(const t of['085_carrier_sla_inbox','Carrier SLA inbox','Release 085','<td>085</td>'])assert.match(x,new RegExp(t,'i'))});
