import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('migration adds audited rescheduling and component shortage intelligence',async()=>{const m=await read('database/migrations/068_vkit_backorder_exceptions.sql'),runner=await read('scripts/migrate.mjs');assert.match(m,/original_requested_ship_date/);assert.match(m,/vkit_backorder_reschedules/);assert.match(m,/CREATE VIEW vkit_backorder_shortages/);assert.match(m,/shortage_quantity/);assert.match(m,/substitute_sku/);assert.match(m,/overdue/);assert.match(runner,/068_vkit_backorder_exceptions/)});
test('exception center filters lifecycle states and explains component blockers',async()=>{const page=await read('app/app/orders/backorders/page.tsx');for(const status of['waiting','ready','overdue','released','fulfilled','cancelled'])assert.match(page,new RegExp(status));assert.match(page,/component blockers/i);assert.match(page,/substitute/);assert.match(page,/Revise date/)});
test('rescheduling requires authority and records notifications and audit',async()=>{const route=await read('app/api/orders/[id]/backorder/reschedule/route.ts');assert.match(route,/assertWarehouseAccess/);assert.match(route,/vkit_backorder_reschedules/);assert.match(route,/warehouse_notifications/);assert.match(route,/store_notifications/);assert.match(route,/vkit_backorder_rescheduled/)});
