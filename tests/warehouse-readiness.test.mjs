import assert from'node:assert/strict';import test from'node:test';import pg from'pg';const url=process.env.DATABASE_ADMIN_URL;
test('latest warehouse migration and operational controls are installed',async()=>{const c=new pg.Client({connectionString:url});await c.connect();try{assert.equal((await c.query(`SELECT version FROM schema_migrations ORDER BY applied_at DESC LIMIT 1`)).rows[0].version,'034_approval_escalation_delivery');const tables=['approval_rules','approval_requests','approval_delegations','approval_notifications','shipments'];const found=(await c.query(`SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name=ANY($1::text[])`,[tables])).rows.map(x=>x.table_name);assert.deepEqual(new Set(found),new Set(tables))}finally{await c.end()}});
test('production safety flags are explicit',()=>{assert.ok(process.env.SESSION_SECRET?.length>=32);assert.match(process.env.APP_URL||'',/^https:\/\//)});
