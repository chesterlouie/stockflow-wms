import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('store request options include virtual kits and component-driven availability',async()=>{const route=await read('app/api/store-request-options/route.ts');assert.match(route,/i\.item_type/);assert.match(route,/virtual_kit/);assert.match(route,/kit_availability/);assert.match(route,/rs\.default_warehouse_id/);assert.match(route,/i\.item_type<>'virtual_kit'/)});
test('store UI identifies VKIT quantity and complete-kit availability',async()=>{const runtime=await read('app/components/MobileRuntime.tsx');assert.match(runtime,/VKIT/);assert.match(runtime,/complete kits available/);assert.match(runtime,/available_kits/)});
test('warehouse acceptance blocks virtual kit demand beyond component capacity',async()=>{const decision=await read('app/api/stores/requests/[id]/decision/route.ts'),runtime=await read('app/components/MobileRuntime.tsx');assert.match(decision,/p\.data\.decision==='accepted'/);assert.match(decision,/kit_availability/);assert.match(decision,/KIT_STOCK/);assert.match(decision,/available_kits/);assert.match(runtime,/complete kits available from component stock/)});
test('existing allocation explodes VKIT demand and scans actual components',async()=>{const allocation=await read('app/api/orders/[id]/allocate/route.ts'),mobile=await read('app/app/fulfillment/mobile/page.tsx');assert.match(allocation,/line\.item_type==='virtual_kit'/);assert.match(allocation,/item_kit_components/);assert.match(allocation,/kit_component/);assert.match(allocation,/pick_tasks/);assert.match(mobile,/Item barcode/)});
test('store and warehouse guides cover the VKIT lifecycle',async()=>{const store=await read('app/store-portal/knowledge/page.tsx'),warehouse=await read('app/app/help/module-eight.tsx');assert.match(store,/Virtual kits/);assert.match(store,/component pick tasks/);assert.match(warehouse,/complete kits available/);assert.match(warehouse,/component-level pick tasks/)});
