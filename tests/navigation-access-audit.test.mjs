import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('warehouse navigation exposes Store approvals and VKIT return receiving to managers',async()=>{const n=await read('app/app/layout.tsx');assert.match(n,/\/app\/stores\/transactions','✓','Store approvals/);assert.match(n,/\/app\/returns\/store-vkits','↩','Store return receiving/);assert.match(n,/manager:new Set\([^\n]+\/app\/stores\/transactions/);assert.match(n,/manager:new Set\([^\n]+\/app\/returns\/store-vkits/)});
test('legacy Store access page redirects to authoritative access scopes',async()=>{const p=await read('app/app/orders/store-access/page.tsx');assert.match(p,/redirect\('\/app\/users\/access'\)/);assert.doesNotMatch(p,/store_user_assignments/)});
test('knowledge and technical records describe navigation decisions',async()=>{const[k,h]=await Promise.all([read('app/app/help/module-eight.tsx'),read('docs/WAREVANTA-TECHNICAL-HANDBOOK.html')]);assert.match(k,/Operations → Store approvals/);assert.match(k,/Inbound → Store return receiving/);assert.match(h,/Navigation access audit/)});
