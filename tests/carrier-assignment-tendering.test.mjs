import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('migration 078 records recommendations assignments events and handover',async()=>{const x=await read('database/migrations/078_carrier_assignment_tendering.sql');for(const t of['order_carrier_assignments','carrier_assignment_events','dispatch_service_recommendations','handover_scan','promised_dispatch_at','ENABLE ROW LEVEL SECURITY'])assert.match(x,new RegExp(t))});
test('mobile dispatch plans a service and enforces override evidence',async()=>{const x=(await read('app/app/dispatch/mobile/page.tsx'))+(await read('app/api/orders/[id]/carrier-assignment/route.ts'))+(await read('app/api/orders/[id]/dispatch/route.ts'));for(const t of['recommended_service','overrideReason','carrierServiceId','order_carrier_assignments','expected_delivery_at'])assert.match(x,new RegExp(t))});
test('handover and late pickup monitoring are wired',async()=>{const x=(await read('app/api/manifests/[id]/close/route.ts'))+(await read('scripts/report-worker.mjs'))+(await read('docs/WAREVANTA-TECHNICAL-HANDBOOK.html'));for(const t of['handoverScan','picked_up','processCarrierTenderExceptions','late_carrier_pickup','Release 078'])assert.match(x,new RegExp(t))});
