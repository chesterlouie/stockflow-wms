import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('owners and administrators control exclusive warehouse or store scopes',async()=>{const api=await read('app/api/users/[id]/access/route.ts');assert.match(api,/owner/);assert.match(api,/admin/);assert.match(api,/UPDATE company_members SET role='viewer'/);assert.match(api,/previousCompanyRole/);assert.match(api,/store_viewer/);assert.match(api,/store_operator/);assert.match(api,/store_manager/);assert.match(api,/storeId&&warehouseIds\.length/);assert.match(api,/DELETE FROM auth_sessions/)});
test('store profiles land in their restricted portal after password or MFA login',async()=>{for(const p of ['app/api/auth/signin/route.ts','app/api/auth/two-factor/route.ts']){const source=await read(p);assert.match(source,/store_user_assignments/);assert.match(source,/withTenant/);assert.match(source,/store-portal/)}});
test('assigned store viewers cannot fall back into the warehouse application shell',async()=>{const source=await read('app/app/layout.tsx');assert.match(source,/session\.role==='viewer'/);assert.match(source,/store_user_assignments/);assert.match(source,/withTenant\(session\.companyId/);assert.match(source,/redirect\('\/store-portal'\)/)});
test('store roles can be selected during invitation and become active on acceptance',async()=>{const migration=await read('database/migrations/052_store_role_invitations.sql'),users=await read('app/api/users/route.ts'),accept=await read('app/api/invitations/[id]/accept/route.ts'),page=await read('app/app/users/page.tsx');for(const role of ['store_viewer','store_operator','store_manager']){assert.match(migration,new RegExp(role));assert.match(users,new RegExp(role));assert.match(page,new RegExp(role))}assert.match(accept,/INSERT INTO store_user_assignments/);assert.match(users,/companyRole=isStoreRole\?'viewer'/);assert.match(page,/Effective access/)});
