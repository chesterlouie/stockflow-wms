import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');
test('store request API accepts and validates multiple item lines',async()=>{const route=await read('app/api/store-requests/route.ts');assert.match(route,/getAll\('itemId'\)/);assert.match(route,/getAll\('quantity'\)/);assert.match(route,/getAll\('requestUom'\)/);assert.match(route,/itemIds\.length>50/);assert.match(route,/const key=`\$\{line\.itemId\}:\$\{line\.uom\}`/);assert.match(route,/totals\.get\(key\)/);assert.match(route,/for\(let index=0;index<lines\.length;index\+\+\)/);assert.match(route,/index\+1/);assert.match(route,/item\.base_uom/)});
test('store request UI provides add and remove line controls',async()=>{const runtime=await read('app/components/MobileRuntime.tsx');assert.match(runtime,/form\[action\$="\/api\/store-requests"\]/);assert.match(runtime,/Add another item/);assert.match(runtime,/Remove stock request line/);assert.match(runtime,/lines\.children\.length >= 50/)});
test('store guide explains batch request behavior and approval scope',async()=>{const guide=await read('app/store-portal/knowledge/page.tsx');assert.match(guide,/Add another item/);assert.match(guide,/combines those lines and sums their quantities/);assert.match(guide,/up to 50 submitted item lines/);assert.match(guide,/decision applies to the full request/)});
test('alternate request UOMs expose barcodes and preserve entered values',async()=>{const options=await read('app/api/store-request-options/route.ts'),route=await read('app/api/store-requests/route.ts'),migration=await read('database/migrations/061_store_request_entered_uom.sql'),runtime=await read('app/components/MobileRuntime.tsx');assert.match(options,/item_uom_conversions/);assert.match(options,/item_barcodes/);assert.match(options,/store_user_assignments/);assert.match(route,/entered_quantity,entered_uom,entered_conversion_factor/);assert.match(route,/units_per_base/);assert.match(migration,/CREATE OR REPLACE FUNCTION normalize_item_uom/);assert.match(runtime,/Request unit/);assert.match(runtime,/Barcode/)});
