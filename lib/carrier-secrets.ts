async function key(){const raw=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(`${process.env.SESSION_SECRET||''}:carrier-integrations`));return crypto.subtle.importKey('raw',raw,'AES-GCM',false,['encrypt','decrypt'])}export async function encryptCarrierSecret(secret:string){const iv=crypto.getRandomValues(new Uint8Array(12)),data=new Uint8Array(await crypto.subtle.encrypt({name:'AES-GCM',iv},await key(),new TextEncoder().encode(secret)));return `${Buffer.from(iv).toString('base64url')}.${Buffer.from(data).toString('base64url')}`}export async function decryptCarrierSecret(value:string){const[a,b]=value.split('.'),iv=Uint8Array.from(Buffer.from(a,'base64url')),data=Uint8Array.from(Buffer.from(b,'base64url'));return new TextDecoder().decode(await crypto.subtle.decrypt({name:'AES-GCM',iv},await key(),data))}export async function verifyCarrierSignature(body:string,signature:string|null,secret:string){if(!signature)return false;const k=await crypto.subtle.importKey('raw',new TextEncoder().encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']),expected=Buffer.from(await crypto.subtle.sign('HMAC',k,new TextEncoder().encode(body))).toString('hex');if(expected.length!==signature.length)return false;let diff=0;for(let i=0;i<expected.length;i++)diff|=expected.charCodeAt(i)^signature.charCodeAt(i);return diff===0}
