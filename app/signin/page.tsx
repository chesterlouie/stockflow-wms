import Link from "next/link";
export const metadata={title:"Sign in"};
export default async function SignIn({searchParams}:{searchParams:Promise<{error?:string}>}){
  const {error}=await searchParams;
  const message=error==='company'?'This company is frozen or its subscription has ended. Contact the StockFlow administrator.':error==='locked'?'Too many attempts. Please wait 15 minutes and try again.':'The email or password could not be verified.';
  return <main className="auth-page"><section className="auth-brand-panel"><Link href="/" className="brand"><span className="brand-mark">S</span><span>StockFlow</span></Link><div className="auth-message"><h1>Your warehouse, ready when you are.</h1><p>Sign in to receive stock, manage orders, scan barcodes, run inventory counts, and keep every movement traceable.</p></div><small>Secure company workspaces · Role-based access</small></section><section className="auth-form-side"><div className="auth-card"><h2>Welcome back</h2><p>Sign in to your company workspace.</p>{error&&<div className="form-error">{message}</div>}<form className="form-stack" method="post" action="/api/auth/signin"><div className="field"><label htmlFor="email">Work email</label><input id="email" name="email" type="email" autoComplete="email" placeholder="you@company.com" required/></div><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" type="password" autoComplete="current-password" placeholder="Enter your password" required/></div><button className="button button-primary" type="submit">Sign in</button></form><div className="auth-switch">New to StockFlow? <Link href="/signup">Create an account</Link></div></div></section></main>
}
