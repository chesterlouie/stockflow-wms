import {db} from '../../../lib/db';import {hashToken} from '../../../lib/tokens';
export const dynamic='force-dynamic';export default async function Verify({params}:{params:Promise<{token:string}>}){const {token}=await params;const valid=(await db().query('SELECT 1 FROM email_verification_tokens WHERE token_hash=$1 AND used_at IS NULL AND expires_at>now()',[await hashToken(token)])).rowCount;return <main className="auth-page"><section className="auth-brand-panel"><div className="brand"><span className="brand-mark">W</span><span>Warevanta</span></div><div className="auth-message"><h1>Activate your workspace.</h1><p>Confirm ownership of your email address to continue.</p></div></section><section className="auth-form-side"><div className="auth-card">{valid?<><h2>Email verification</h2><form method="post" action={`/api/auth/verify-email/${token}`}><button className="button button-primary">Verify email address</button></form></>:<><h2>Link unavailable</h2><p>This verification link has expired or was already used.</p></>}</div></section></main>}
