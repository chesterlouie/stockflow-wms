import{db}from'../../../lib/db';import{hashToken}from'../../../lib/tokens';export const dynamic='force-dynamic';export default async function CarrierTenderResponse({params,searchParams}:{params:Promise<{token:string}>;searchParams:Promise<Record<string,string|undefined>>}){const{token}=await params,q=await searchParams,x=(await db().query(`SELECT * FROM resolve_carrier_tender_token($1)`,[await hashToken(token)])).rows[0];if(!x)return <main className="auth-page"><section className="auth-card"><h1>Tender link unavailable</h1><p>This secure link is invalid, expired, revoked, or already used.</p></section></main>;return <main className="auth-page"><section className="auth-card"><h1>Carrier tender</h1>{q.created&&<div className="success-banner">Secure response link created. Share only with the intended carrier.</div>}<p><strong>{x.carrier_name} · {x.service_level}</strong></p><dl><dt>Order</dt><dd>{x.order_no}</dd><dt>Warehouse</dt><dd>{x.warehouse_name}</dd><dt>Destination</dt><dd>{x.store_name}</dd><dt>Units</dt><dd>{x.shipment_units}</dd><dt>Response due</dt><dd>{new Date(x.response_due_at).toLocaleString()}</dd></dl><form className="form-stack" method="post" action={`/api/carrier-tender/${token}/respond`}><label>Response note<input name="note" maxLength={500}/></label><div className="form-row"><button name="response" value="accepted">Accept tender</button><button name="response" value="rejected">Reject tender</button></div></form></section></main>}
