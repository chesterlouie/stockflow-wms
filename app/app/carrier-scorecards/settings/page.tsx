import{redirect}from'next/navigation';import Link from'next/link';import{getSession}from'../../../../lib/auth';import{tenantRows}from'../../../../lib/db';type Config={name:string;delivery_weight:string;cost_weight:string;quality_weight:string;invoice_weight:string;claims_weight:string};export default async function ScorecardSettings(){const s=await getSession();if(!s||!['owner','admin'].includes(s.role))redirect('/app/restricted');const current=(await tenantRows<Config>(s.companyId,`SELECT name,delivery_weight,cost_weight,quality_weight,invoice_weight,claims_weight FROM carrier_scorecard_configs WHERE company_id=$1 AND active ORDER BY created_at DESC LIMIT 1`,[s.companyId]))[0];return <div className="app-content"><div className="page-heading"><div><h1>Scorecard settings</h1><p>Set the relative importance of each carrier-performance dimension.</p></div><Link className="button button-secondary" href="/app/carrier-scorecards">Back to scorecards</Link></div><section className="panel"><form className="form-stack" method="post" action="/api/carrier-scorecards/config"><input name="name" placeholder="Configuration name" defaultValue={current?.name||'Weighted carrier score'} required/><div className="form-row"><label>Delivery weight %<input name="deliveryWeight" type="number" min="0" step=".001" defaultValue={current?.delivery_weight||30} required/></label><label>Cost weight %<input name="costWeight" type="number" min="0" step=".001" defaultValue={current?.cost_weight||25} required/></label><label>Quality weight %<input name="qualityWeight" type="number" min="0" step=".001" defaultValue={current?.quality_weight||20} required/></label><label>Invoice weight %<input name="invoiceWeight" type="number" min="0" step=".001" defaultValue={current?.invoice_weight||10} required/></label><label>Claims weight %<input name="claimsWeight" type="number" min="0" step=".001" defaultValue={current?.claims_weight||15} required/></label></div><p>The five weights must total exactly 100%.</p><button className="button button-primary">Activate new score version</button></form></section></div>}
