import{getSession}from'../../../lib/auth';import{tenantRows}from'../../../lib/db';export const dynamic='force-dynamic';type Q={id:string;operation_type:string;entity_id:string;metric_quantity:string|null;status:string;current_step:string;requested_at:string;requester:string;approver_role:string;rule_name:string;escalation_hours:string};type Rule={id:string;name:string;operation_type:string;required_steps:string;active:boolean};export default async function Approvals(){const s=await getSession();const rows=s?await tenantRows<Q>(s.companyId,`SELECT q.id,q.operation_type,q.entity_id,q.metric_quantity::text,q.status,q.current_step::text,q.requested_at::text,u.display_name requester,rs.approver_role,r.name rule_name,r.escalation_hours::text FROM approval_requests q JOIN approval_rules r ON r.id=q.rule_id JOIN approval_rule_steps rs ON rs.rule_id=r.id AND rs.step_no=q.current_step LEFT JOIN users u ON u.id=q.requested_by WHERE q.company_id=$1 ORDER BY q.status='pending' DESC,q.requested_at`,[s.companyId]):[];const rules=s?await tenantRows<Rule>(s.companyId,`SELECT id,name,operation_type,required_steps::text,active FROM approval_rules WHERE company_id=$1 ORDER BY name`,[s.companyId]):[];const admin=Boolean(s&&['owner','admin'].includes(s.role));return <div className="app-content"><div className="page-heading"><div><h1>Approval workflows</h1><p>Risk-based requests, multi-step decisions, and complete audit history.</p></div></div><div className="form-layout"><section className="panel"><h2>Approval inbox</h2>{rows.length?rows.map(q=><article className="pick-card exception-card" key={q.id}><div><span className="badge">{q.status}</span><strong>{q.operation_type.replaceAll('_',' ')} · {q.entity_id}</strong><small>{q.rule_name} · Step {q.current_step} · {q.requester||'User'}</small><small>{q.metric_quantity&&`Quantity ${q.metric_quantity} · `}{new Date(q.requested_at).toLocaleString()}</small></div>{q.status==='pending'&&q.approver_role===s?.role&&<form className="form-stack compact-form" method="post" action={`/api/approvals/${q.id}/decision`}><input name="comment" placeholder="Required for rejection or correction"/><div className="form-row"><button name="decision" value="approved" className="button button-primary">Approve</button><button name="decision" value="returned" className="button button-secondary">Return</button><button name="decision" value="rejected" className="button button-secondary">Reject</button></div></form>}</article>):<p className="empty-cell">No approval requests.</p>}</section>{admin&&<aside className="panel"><h2>Create approval rule</h2><form className="form-stack" method="post" action="/api/approvals/rules"><input name="name" placeholder="Large adjustment approval" required/><select name="operationType"><option value="inventory_adjustment">Inventory adjustment</option><option value="count_variance">Count variance</option><option value="order_cancel">Order cancellation</option><option value="order_reopen">Order reopening</option><option value="dispatch_reversal">Dispatch reversal</option><option value="return_reversal">Return reversal</option><option value="forecast_replenishment">Forecast replenishment</option><option value="purchase_order_release">Purchase order release</option></select><input name="thresholdQuantity" type="number" min="0" step="any" placeholder="Quantity threshold (optional)"/><select name="requiredSteps" defaultValue="1"><option value="1">1 approval step</option><option value="2">2 approval steps</option></select><select name="step1Role"><option value="manager">Manager</option><option value="admin">Administrator</option><option value="owner">Owner</option></select><select name="step2Role"><option value="owner">Owner</option><option value="admin">Administrator</option></select><input name="escalationHours" type="number" min="1" max="720" defaultValue="24"/><button className="button button-primary">Create rule</button></form><h2>Active rules</h2>{rules.map(r=><div className="task" key={r.id}><span><strong>{r.name}</strong><small>{r.operation_type.replaceAll('_',' ')} · {r.required_steps} step(s)</small></span></div>)}</aside>}</div></div>}
