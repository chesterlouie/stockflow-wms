import Link from'next/link';import{getSession}from'../../../../lib/auth';import{tenantRows}from'../../../../lib/db';export const dynamic='force-dynamic';
type Backorder={id:string;order_no:string;parent_order_no:string;store:string;warehouse:string;requested_ship_date:string|null;original_requested_ship_date:string|null;backorder_status:string;age_days:string;overdue:boolean;lines:string;shortage_lines:string};type Shortage={order_id:string;component_sku:string;required_quantity:string;available_quantity:string;shortage_quantity:string;substitute_sku:string|null;substitute_available_quantity:string|null};
export default async function Backorders({searchParams}:{searchParams:Promise<{status?:string;updated?:string;error?:string}>}){const s=await getSession(),q=await searchParams,status=['waiting','ready','released','fulfilled','cancelled','overdue'].includes(q.status||'')?q.status!:'';const[rows,shortages]=s?await Promise.all([tenantRows<Backorder>(s.companyId,`SELECT o.id,o.order_no,p.order_no parent_order_no,x.name store,w.name warehouse,o.requested_ship_date::text,o.original_requested_ship_date::text,o.backorder_status,(current_date-o.created_at::date)::text age_days,(o.requested_ship_date<current_date AND o.backorder_status IN('waiting','ready')) overdue,string_agg(i.sku||' × '||l.ordered_quantity::text||' '||l.uom,', ' ORDER BY l.line_no) lines,count(*) FILTER(WHERE bs.shortage_quantity>0)::text shortage_lines FROM sales_orders o JOIN sales_orders p ON p.company_id=o.company_id AND p.id=o.parent_order_id JOIN warehouses w ON w.company_id=o.company_id AND w.id=o.warehouse_id LEFT JOIN requesting_stores x ON x.company_id=o.company_id AND x.id=o.store_id JOIN sales_order_lines l ON l.company_id=o.company_id AND l.order_id=o.id JOIN items i ON i.company_id=l.company_id AND i.id=l.item_id LEFT JOIN vkit_backorder_shortages bs ON bs.company_id=o.company_id AND bs.order_id=o.id WHERE o.company_id=$1 AND ($2='' OR o.backorder_status=$2 OR ($2='overdue' AND o.requested_ship_date<current_date AND o.backorder_status IN('waiting','ready'))) AND ($3='owner' OR EXISTS(SELECT 1 FROM user_warehouse_assignments a WHERE a.company_id=o.company_id AND a.user_id=$4 AND a.warehouse_id=o.warehouse_id)) GROUP BY o.id,p.order_no,x.name,w.name ORDER BY overdue DESC,o.requested_ship_date NULLS LAST,o.created_at`,[s.companyId,status,s.role,s.userId]),tenantRows<Shortage>(s.companyId,`SELECT order_id,component_sku,required_quantity::text,available_quantity::text,shortage_quantity::text,substitute_sku,substitute_available_quantity::text FROM vkit_backorder_shortages WHERE company_id=$1 AND shortage_quantity>0 ORDER BY component_sku`,[s.companyId])]):[[],[]];const count=(key:string)=>rows.filter(x=>key==='overdue'?x.overdue:x.backorder_status===key).length;return <div className="app-content"><div className="page-heading"><div><h1>VKIT backorder exceptions</h1><p>Prioritize shortages, overdue demand, substitutes, and revised commitments.</p></div><Link className="button button-secondary" href="/app/orders">Order management</Link></div>{q.updated&&<div className="success-banner">Backorder required date revised and concerned users notified.</div>}{q.error&&<div className="form-error">The backorder could not be rescheduled. Confirm the date, reason, and current status.</div>}<div className="form-row">{['waiting','ready','overdue','released','fulfilled','cancelled'].map(x=><Link key={x} className="button button-secondary" href={`/app/orders/backorders?status=${x}`}>{x} · {count(x)}</Link>)}</div><section className="panel"><table className="data-table"><thead><tr><th>Backorder</th><th>Store / warehouse</th><th>Outstanding</th><th>Age / required</th><th>Status</th><th>Component blockers</th><th>Commitment</th></tr></thead><tbody>{rows.length?rows.map(o=>{const blocked=shortages.filter(x=>x.order_id===o.id);return <tr key={o.id}><td><Link className="table-link" href={`/app/orders/${o.id}`}>{o.order_no}</Link><small>Parent {o.parent_order_no}</small></td><td>{o.store||'External customer'}<small>{o.warehouse}</small></td><td>{o.lines}</td><td>{o.age_days} day(s)<small>{o.requested_ship_date||'No date'}{o.overdue?' · OVERDUE':''}</small></td><td><span className="badge">{o.backorder_status}</span></td><td>{blocked.length?blocked.map(x=><small key={x.component_sku}><strong>{x.component_sku}</strong>: short {x.shortage_quantity} (need {x.required_quantity}, ATP {x.available_quantity}){x.substitute_sku?` · substitute ${x.substitute_sku} ATP ${x.substitute_available_quantity||0}`:''}</small>):<small>All components buildable</small>}</td><td>{['waiting','ready'].includes(o.backorder_status)?<form className="form-stack compact-form" method="post" action={`/api/orders/${o.id}/backorder/reschedule`}><input type="date" name="requiredDate" required/><input name="reason" placeholder="Reason for revised date" required/><button className="button button-secondary">Revise date</button></form>:<span>—</span>}</td></tr>}):<tr><td className="empty-cell" colSpan={7}>No backorders match this filter.</td></tr>}</tbody></table></section></div>}
