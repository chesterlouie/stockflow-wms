import Link from'next/link';import{redirect}from'next/navigation';import{getSession}from'../../../../../lib/auth';export default async function CarrierInvoiceImport(){const s=await getSession();if(!s||!['owner','admin'].includes(s.role))redirect('/app/restricted');return <div className="app-content"><div className="page-heading"><div><h1>Import carrier invoice CSV</h1><p>Create invoice headers and auto-match shipment charge lines in one controlled transaction.</p></div><Link className="button button-secondary" href="/app/freight/reconciliation">Back to reconciliation</Link></div><section className="panel"><p>Required columns: <code>carrier_code, invoice_number, invoice_date, due_date, currency, total_amount, shipment_reference, billed_amount, description</code>. Repeat invoice header values for each line.</p><form className="form-stack" method="post" action="/api/freight/invoices/import"><textarea name="csv" rows={16} required placeholder="carrier_code,invoice_number,invoice_date,due_date,currency,total_amount,shipment_reference,billed_amount,description"/><button className="button button-primary">Validate and import</button></form></section></div>}
