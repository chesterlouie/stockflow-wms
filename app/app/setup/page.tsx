import { getSession } from "../../../lib/auth";
import { tenantRows } from "../../../lib/db";
export const metadata={title:'Warehouse setup'};export const dynamic='force-dynamic';
type Warehouse={id:string;code:string;name:string};type Location={id:string;warehouse_name:string;code:string;type:string};
export default async function Setup({searchParams}:{searchParams:Promise<{created?:string;error?:string}>}){const session=await getSession();const params=await searchParams;const warehouses=session?await tenantRows<Warehouse>(session.companyId,'SELECT id,code,name FROM warehouses WHERE company_id=$1 ORDER BY name',[session.companyId]):[];const locations=session?await tenantRows<Location>(session.companyId,`SELECT l.id,w.name AS warehouse_name,l.code,l.type FROM locations l JOIN warehouses w ON w.company_id=l.company_id AND w.id=l.warehouse_id WHERE l.company_id=$1 ORDER BY w.name,l.code`,[session.companyId]):[];return <div className="app-content"><div className="page-heading"><div><h1>Warehouse setup</h1><p>Configure scannable operational and storage locations.</p></div></div>{params.created&&<div className="success-banner">Location created successfully.</div>}{params.error&&<div className="form-error">{params.error==='duplicate'?'That location code already exists in this warehouse.':'Please check the location details.'}</div>}<div className="form-layout"><section className="panel"><h2>Locations</h2><div style={{overflowX:'auto'}}><table className="data-table"><thead><tr><th>Warehouse</th><th>Location</th><th>Type</th><th>Status</th></tr></thead><tbody>{locations.length?locations.map(location=><tr key={location.id}><td>{location.warehouse_name}</td><td><strong>{location.code}</strong></td><td>{location.type}</td><td><span className="badge">Active</span></td></tr>):<tr><td colSpan={4} className="empty-cell">Create a receiving or storage location to begin.</td></tr>}</tbody></table></div></section><aside className="panel"><h2>Create location</h2><form className="form-stack compact-form" method="post" action="/api/locations"><div className="field"><label htmlFor="warehouseId">Warehouse</label><select id="warehouseId" name="warehouseId" required>{warehouses.map(warehouse=><option key={warehouse.id} value={warehouse.id}>{warehouse.code} — {warehouse.name}</option>)}</select></div><div className="field"><label htmlFor="code">Location code</label><input id="code" name="code" placeholder="e.g. REC-01 or A-01-02" required/></div><div className="field"><label htmlFor="type">Location type</label><select id="type" name="type"><option value="receiving">Receiving</option><option value="storage">Storage</option><option value="picking">Picking</option><option value="packing">Packing</option><option value="shipping">Shipping</option><option value="hold">Hold</option><option value="damaged">Damaged</option></select></div><button className="button button-primary" type="submit" disabled={!warehouses.length}>Create location</button></form></aside></div></div>}
