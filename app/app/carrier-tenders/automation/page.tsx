import Link from'next/link';import{getSession}from'../../../../lib/auth';import{tenantRows}from'../../../../lib/db';type Config={max_attempts:number;escalation_minutes:number;auto_retender_on_rejection:boolean;auto_retender_on_expiry:boolean;enabled:boolean};export default async function TenderAutomation({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const s=await getSession(),q=await searchParams;if(!s)return null;const cfg=(await tenantRows<Config>(s.companyId,`SELECT max_attempts,escalation_minutes,auto_retender_on_rejection,auto_retender_on_expiry,enabled FROM carrier_tender_automation_configs WHERE company_id=$1`,[s.companyId]))[0];return <div className="app-content"><div className="page-heading"><div><h1>Carrier tender automation</h1><p>Control automatic fallback, attempt limits, and escalation timing.</p></div><Link className="button button-secondary" href="/app/carrier-tenders">Back to tenders</Link></div>{q.saved&&<div className="success-banner">Tender automation policy saved.</div>}{q.error&&<div className="form-error">Automation policy could not be saved.</div>}<section className="panel"><form className="form-stack" method="post" action="/api/carrier-tenders/automation"><label>Maximum tender attempts<input name="maxAttempts" type="number" min="1" max="20" defaultValue={cfg?.max_attempts||3} required/></label><label>Escalate after exhausted options (minutes)<input name="escalationMinutes" type="number" min="5" max="1440" defaultValue={cfg?.escalation_minutes||30} required/></label><label><input name="autoRetenderOnRejection" type="checkbox" defaultChecked={cfg?.auto_retender_on_rejection??true}/> Automatically retender after carrier rejection</label><label><input name="autoRetenderOnExpiry" type="checkbox" defaultChecked={cfg?.auto_retender_on_expiry??true}/> Automatically retender after response expiry</label><label><input name="enabled" type="checkbox" defaultChecked={cfg?.enabled??true}/> Enable automation</label><button disabled={!['owner','admin'].includes(s.role)}>Save automation policy</button></form></section></div>}
