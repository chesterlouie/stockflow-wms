import Link from 'next/link';import {getSession} from '../../../../lib/auth';import {tenantRows} from '../../../../lib/db';
export const dynamic='force-dynamic';type Task={id:string;receipt_no:string;external_reference:string|null;supplier:string;sku:string;description:string;remaining:string;uom:string;status:string};
export default async function MobileReceiving({searchParams}:{searchParams:Promise<{barcode?:string}>}){const s=await getSession();const q=await searchParams;const barcode=(q.barcode||'').trim();const tasks=s?await tenantRows<Task>(s.companyId,`SELECT r.id,r.receipt_no,r.external_reference,r.supplier,i.sku,i.description,(rl.expected_quantity-rl.accepted_quantity-rl.held_quantity-rl.damaged_quantity)::text AS remaining,rl.uom,r.status FROM inbound_receipts r JOIN inbound_receipt_lines rl ON rl.receipt_id=r.id JOIN items i ON i.id=rl.item_id LEFT JOIN item_barcodes b ON b.item_id=i.id AND b.company_id=i.company_id WHERE r.company_id=$1 AND r.status IN('expected','partial') AND ($2='' OR r.receipt_no=$2 OR r.external_reference=$2 OR i.sku=$2 OR b.barcode_value=$2) GROUP BY r.id,i.id,rl.id ORDER BY r.expected_date NULLS LAST,r.created_at LIMIT 30`,[s.companyId,barcode]):[];return <div className="app-content"><div className="page-heading"><div><h1>Mobile receiving</h1><p>Scan a receipt label, PO number, SKU, or item barcode.</p></div><Link href="/app/receiving" className="button button-secondary">Desktop receiving</Link></div><form className="panel form-stack" method="get"><div className="field"><label>Receipt, PO, or item barcode</label><input name="barcode" defaultValue={barcode} autoFocus placeholder="Scan to find inbound task" required/></div><button className="button button-primary">Find receiving task</button></form><section className="panel inventory-history"><h2>{barcode?'Matching open tasks':'Next open tasks'}</h2>{tasks.length?tasks.map(t=><Link key={t.id} href={`/app/receiving/mobile/${t.id}`} className="pick-card"><div><strong>{t.receipt_no} · {t.sku}</strong><small>{t.supplier} · PO {t.external_reference||'—'}</small><small>{t.description}</small></div><span className="badge">{t.remaining} {t.uom} remaining</span></Link>):<p className="empty-cell">No open receiving task matches that scan.</p>}</section></div>}
