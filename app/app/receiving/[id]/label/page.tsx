import {notFound} from 'next/navigation';import {getSession} from '../../../../../lib/auth';import {tenantRows} from '../../../../../lib/db';import QrCode from '../../../../components/QrCode';
export const dynamic='force-dynamic';export default async function Label({params}:{params:Promise<{id:string}>}){const s=await getSession();const {id}=await params;const row=(s?await tenantRows<{receipt_no:string;supplier:string;sku:string;description:string;expected_quantity:string;uom:string;external_reference:string|null}>(s.companyId,`SELECT r.receipt_no,r.supplier,r.external_reference,i.sku,i.description,rl.expected_quantity::text,rl.uom FROM inbound_receipts r JOIN inbound_receipt_lines rl ON rl.receipt_id=r.id JOIN items i ON i.id=rl.item_id AND i.company_id=r.company_id WHERE r.company_id=$1 AND r.id=$2`,[s.companyId,id]):[])[0];if(!row)notFound();return <main style={{maxWidth:620,margin:'40px auto',fontFamily:'system-ui'}}><section style={{border:'3px solid #111',padding:28}}><small>WAREVANTA RECEIVING</small><h1 style={{fontSize:42,margin:'10px 0'}}>{row.receipt_no}</h1><p><strong>PO/reference:</strong> {row.external_reference||'—'}</p><p><strong>Supplier:</strong> {row.supplier}</p><hr/><h2>{row.sku}</h2><p>{row.description}</p><p style={{fontSize:28}}>{row.expected_quantity} {row.uom}</p><div style={{display:'grid',placeItems:'center',marginTop:25}}><QrCode value={row.receipt_no}/><strong>{row.receipt_no}</strong></div></section><p>Use your browser&apos;s Print command to print this scannable receiving label.</p></main>}
