import Link from 'next/link';
export default async function Required({searchParams}:{searchParams:Promise<{sent?:string}>}){const q=await searchParams;return <main className="auth-page"><section className="auth-brand-panel"><Link href="/" className="brand"><span className="brand-mark">W</span><span>Warevanta</span></Link><div className="auth-message"><h1>Check your inbox.</h1><p>Email verification protects each company workspace from unauthorized registrations.</p></div></section><section className="auth-form-side"><div className="auth-card"><h2>Verify your email</h2>{q.sent&&<div className="success-banner">If the address belongs to an unverified account, a new 24-hour link has been sent.</div>}<p>Open the link in your email, then return to sign in.</p><form className="form-stack" method="post" action="/api/auth/resend-verification"><div className="field"><label>Work email</label><input name="email" type="email" required/></div><button className="button button-secondary">Resend verification email</button></form><Link href="/signin">Return to sign in</Link></div></section></main>}
