import Link from "next/link";import { redirect } from "next/navigation";import { getSession } from "../../../lib/auth";
export const metadata={title:'Change password'};export const dynamic='force-dynamic';export default async function Password({searchParams}:{searchParams:Promise<{error?:string}>}){const s=await getSession();if(!s)redirect('/signin');const {error}=await searchParams;return <main className="auth-page"><section className="auth-brand-panel"><Link href="/" className="brand"><span className="brand-mark">S</span><span>StockFlow</span></Link><div className="auth-message"><h1>Protect your warehouse account.</h1><p>Choose a unique password with at least ten characters. All existing sessions will be signed out after the change.</p></div><small>Secure company access</small></section><section className="auth-form-side"><div className="auth-card"><h2>{s.mustChangePassword?'Set your permanent password':'Change password'}</h2><p>Signed in as {s.email}</p>{error&&<div className="form-error">{error==='current'?'The current password is incorrect.':'Passwords must match and contain at least ten characters.'}</div>}<form className="form-stack" method="post" action="/api/account/password"><div className="field"><label>Current password</label><input name="currentPassword" type="password" required/></div><div className="field"><label>New password</label><input name="newPassword" type="password" minLength={10} required/></div><div className="field"><label>Confirm new password</label><input name="confirmPassword" type="password" minLength={10} required/></div><button className="button button-primary">Change password</button></form></div></section></main>}
