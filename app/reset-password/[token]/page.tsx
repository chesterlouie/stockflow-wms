import {db} from '../../../lib/db';import {hashToken} from '../../../lib/tokens';
export const dynamic='force-dynamic';export default async function Reset({params,searchParams}:{params:Promise<{token:string}>;searchParams:Promise<{error?:string}>}){const {token}=await params;const q=await searchParams;const valid=(await db().query('SELECT 1 FROM password_reset_tokens WHERE token_hash=$1 AND used_at IS NULL AND expires_at>now()',[await hashToken(token)])).rowCount;return <main className="auth-page"><section className="auth-brand-panel"><div className="brand"><span className="brand-mark">W</span><span>Warevanta</span></div><div className="auth-message"><h1>Choose a new password.</h1><p>Your reset link is protected, expires in one hour, and works only once.</p></div></section><section className="auth-form-side"><div className="auth-card">{valid?<><h2>Reset password</h2>{q.error&&<div className="form-error">Passwords must match and contain at least ten characters.</div>}<form className="form-stack" method="post" action={`/api/auth/reset-password/${token}`}><div className="field"><label>New password</label><input name="password" type="password" minLength={10} required/></div><div className="field"><label>Confirm password</label><input name="confirmPassword" type="password" minLength={10} required/></div><button className="button button-primary">Save new password</button></form></>:<><h2>Reset link unavailable</h2><p>This link has expired or was already used. Request a new password reset.</p></>}</div></section></main>}
