import Link from "next/link";
export const metadata = { title: "Create workspace" };
export default async function SignUp({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;const message=error==='exists'?'That email already has an account.':error?'The workspace could not be created. Check the details and database connection.':'';return <main className="auth-page"><section className="auth-brand-panel"><Link href="/" className="brand"><span className="brand-mark">S</span><span>StockFlow</span></Link><div className="auth-message"><h1>Give your inventory a reliable home.</h1><p>Create a private company workspace. Your items, barcodes, warehouses, users, reports, and integrations remain isolated from every other subscriber.</p></div><small>Local-first · Cloud-ready · ERP-friendly</small></section><section className="auth-form-side"><div className="auth-card"><h2>Create your workspace</h2><p>Set up your company&apos;s private inventory space.</p>{message&&<div className="form-error">{message}</div>}<form className="form-stack" method="post" action="/api/auth/signup"><div className="field"><label htmlFor="company">Company name</label><input id="company" name="company" autoComplete="organization" placeholder="Your company" required/></div><div className="field"><label htmlFor="email">Work email</label><input id="email" name="email" type="email" autoComplete="email" placeholder="you@company.com" required/></div><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" type="password" autoComplete="new-password" minLength={10} placeholder="At least 10 characters" required/></div><button className="button button-primary" type="submit">Create company workspace</button><div className="form-note">By continuing, you agree to the terms of service and acknowledge the privacy policy.</div></form><div className="auth-switch">Already have an account? <Link href="/signin">Sign in</Link></div></div></section></main>}
