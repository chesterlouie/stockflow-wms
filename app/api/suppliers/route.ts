import {getSession} from '../../../lib/auth';import {withTenant} from '../../../lib/db';import {z} from 'zod';const schema=z.object({code:z.string().trim().min(1).max(30).transform(x=>x.toUpperCase()),name:z.string().trim().min(2).max(150),email:z.string().trim().email().optional().or(z.literal('')),phone:z.string().trim().max(40).optional()});
export async function POST(request:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await request.formData()));if(!p.success)return Response.redirect(new URL('/app/suppliers?error=invalid',request.url),303);try{await withTenant(s.companyId,async c=>{const supplier=(await c.query<{id:string}>('INSERT INTO suppliers(company_id,code,name,email,phone) VALUES($1,$2,$3,$4,$5) RETURNING id',[s.companyId,p.data.code,p.data.name,p.data.email||null,p.data.phone||null])).rows[0];await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id,details) VALUES($1,$2,'supplier_created','supplier',$3,$4::jsonb)`,[s.companyId,s.userId,supplier.id,JSON.stringify({code:p.data.code,name:p.data.name})])});return Response.redirect(new URL('/app/suppliers?created=1',request.url),303)}catch{return Response.redirect(new URL('/app/suppliers?error=duplicate',request.url),303)}}
