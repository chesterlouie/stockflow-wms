import {getSession} from '../../../lib/auth';import {withTenant} from '../../../lib/db';import {z} from 'zod';const schema=z.object({code:z.string().trim().min(1).max(30).transform(x=>x.toUpperCase()),name:z.string().trim().min(2).max(150),email:z.string().trim().email().optional().or(z.literal('')),phone:z.string().trim().max(40).optional()});
export async function POST(request:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await request.formData()));if(!p.success)return Response.redirect(new URL('/app/purchasing?error=supplier',request.url),303);try{await withTenant(s.companyId,c=>c.query('INSERT INTO suppliers(company_id,code,name,email,phone) VALUES($1,$2,$3,$4,$5)',[s.companyId,p.data.code,p.data.name,p.data.email||null,p.data.phone||null]));return Response.redirect(new URL('/app/purchasing?supplier=1',request.url),303)}catch{return Response.redirect(new URL('/app/purchasing?error=duplicate',request.url),303)}}
