import{z}from'zod';import{getSession}from'../../../../lib/auth';import{withTenant}from'../../../../lib/db';const schema=z.object({warehouseId:z.string().uuid(),name:z.string().trim().min(1).max(80),startsAt:z.string().min(1),endsAt:z.string().min(1)});export async function POST(r:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await r.formData()));if(!p.success)return Response.redirect(new URL('/app/labor?error=shift',r.url),303);await withTenant(s.companyId,c=>c.query(`INSERT INTO warehouse_shifts(company_id,warehouse_id,name,starts_at,ends_at,created_by) VALUES($1,$2,$3,$4,$5,$6)`,[s.companyId,p.data.warehouseId,p.data.name,p.data.startsAt,p.data.endsAt,s.userId]));return Response.redirect(new URL('/app/labor?saved=shift',r.url),303)}
