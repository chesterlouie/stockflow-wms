import {z} from 'zod';import {getSession} from '../../../../lib/auth';import {withTenant} from '../../../../lib/db';const schema=z.object({reportType:z.enum(['stock','movements','receiving','fulfillment','expiry','variances']),frequency:z.enum(['daily','weekly','monthly']),format:z.enum(['csv','pdf']),recipients:z.string().trim().min(3).max(1000)});
export async function POST(request:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await request.formData()));if(!p.success)return Response.redirect(new URL('/app/reports?error=schedule',request.url),303);const days=p.data.frequency==='daily'?1:p.data.frequency==='weekly'?7:30;await withTenant(s.companyId,c=>c.query(`INSERT INTO report_schedules(company_id,report_type,frequency,format,recipients,next_run_at,created_by) VALUES($1,$2,$3,$4,$5,now()+($6||' days')::interval,$7)`,[s.companyId,p.data.reportType,p.data.frequency,p.data.format,p.data.recipients,days,s.userId]));return Response.redirect(new URL('/app/reports?saved=schedule',request.url),303)}
