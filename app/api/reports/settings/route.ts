import {z} from 'zod';import {getSession} from '../../../../lib/auth';import {withTenant} from '../../../../lib/db';const schema=z.object({expiryWarningDays:z.coerce.number().int().min(1).max(365),lowStockQuantity:z.coerce.number().min(0),overdueOrderHours:z.coerce.number().int().min(1).max(720),accuracyTarget:z.coerce.number().min(0).max(100)});
export async function POST(request:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await request.formData()));if(!p.success)return Response.redirect(new URL('/app/reports?error=settings',request.url),303);await withTenant(s.companyId,c=>c.query(`INSERT INTO warehouse_alert_settings(company_id,expiry_warning_days,low_stock_quantity,overdue_order_hours,accuracy_target) VALUES($1,$2,$3,$4,$5) ON CONFLICT(company_id) DO UPDATE SET expiry_warning_days=excluded.expiry_warning_days,low_stock_quantity=excluded.low_stock_quantity,overdue_order_hours=excluded.overdue_order_hours,accuracy_target=excluded.accuracy_target,updated_at=now()`,[s.companyId,p.data.expiryWarningDays,p.data.lowStockQuantity,p.data.overdueOrderHours,p.data.accuracyTarget]));return Response.redirect(new URL('/app/reports?saved=settings',request.url),303)}
