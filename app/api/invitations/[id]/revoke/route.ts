import {getSession} from '../../../../../lib/auth';import {withTenant} from '../../../../../lib/db';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin'].includes(s.role))return new Response('Forbidden',{status:403});const {id}=await params;await withTenant(s.companyId,async c=>{const invite=(await c.query(`UPDATE user_invitations SET revoked_at=now() WHERE id=$1 AND company_id=$2 AND accepted_at IS NULL AND revoked_at IS NULL RETURNING email`,[id,s.companyId])).rows[0];if(invite)await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id,details) VALUES($1,$2,'user_invitation_revoked','user_invitation',$3,$4::jsonb)`,[s.companyId,s.userId,id,JSON.stringify({email:invite.email})])});return Response.redirect(new URL('/app/users?updated=revoked',request.url),303)}
