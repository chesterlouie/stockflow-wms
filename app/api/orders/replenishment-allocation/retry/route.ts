import{getSession}from'../../../../../lib/auth';import{withTenant}from'../../../../../lib/db';import{assertWarehouseAccess}from'../../../../../lib/warehouse-access';import{processReplenishmentAllocationJobs}from'../../../../../lib/replenishment-auto-allocation';import{z}from'zod';const schema=z.object({jobId:z.string().uuid(),reason:z.string().trim().min(2)});
export async function POST(r:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await r.formData()));if(!p.success)return go(r,'invalid');try{const count=await withTenant(s.companyId,async c=>{const j=(await c.query(`SELECT * FROM replenishment_allocation_jobs WHERE company_id=$1 AND id=$2 AND status IN('pending','exception') FOR UPDATE`,[s.companyId,p.data.jobId])).rows[0];if(!j)throw new Error('state');await assertWarehouseAccess(c,s,j.warehouse_id);await c.query(`UPDATE replenishment_allocation_jobs SET status='pending',next_attempt_at=now(),last_error=NULL,updated_at=now() WHERE id=$1`,[j.id]);await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id,details) VALUES($1,$2,'replenishment_allocation_retried','replenishment_allocation_job',$3,$4::jsonb)`,[s.companyId,s.userId,j.id,JSON.stringify({reason:p.data.reason})]);return processReplenishmentAllocationJobs(c,s.companyId,j.warehouse_id,s.userId)});return Response.redirect(new URL(`/app/orders/replenishment-allocation?retried=${count}`,r.url),303)}catch{return go(r,'state')}}function go(r:Request,e:string){return Response.redirect(new URL(`/app/orders/replenishment-allocation?error=${e}`,r.url),303)}
