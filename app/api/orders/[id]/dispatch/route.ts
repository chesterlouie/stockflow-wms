import{getSession}from'../../../../../lib/auth';import{withTenant}from'../../../../../lib/db';import{assertOrderAccess}from'../../../../../lib/warehouse-access';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);const{id}=await params,f=Object.fromEntries(await request.formData()),mobile=f.mobile==='1';let shipmentId='';try{if(mobile&&(!(String(f.carrierServiceId||'').trim()||String(f.carrier||'').trim())||!String(f.trackingNumber||'').trim()))throw new Error('details');await withTenant(s.companyId,c=>assertOrderAccess(c,s,id));shipmentId=await withTenant(s.companyId,async c=>{const order=(await c.query(`SELECT * FROM sales_orders WHERE company_id=$1 AND id=$2 AND status='packed' FOR UPDATE`,[s.companyId,id])).rows[0];if(!order)throw new Error('state');const tasks=(await c.query(`SELECT p.*,l.code packing_code,pc.status carton_status FROM pick_tasks p JOIN locations l ON l.id=p.to_location_id JOIN stock_allocations a ON a.id=p.allocation_id JOIN sales_order_lines ol ON ol.id=a.order_line_id LEFT JOIN carton_items ci ON ci.company_id=p.company_id AND ci.pick_task_id=p.id LEFT JOIN packing_cartons pc ON pc.id=ci.carton_id WHERE ol.order_id=$1 AND p.status='packed'`,[id])).rows;if(!tasks.length||tasks.some(t=>t.carton_status!=='sealed'))throw new Error('cartons');if(mobile&&tasks.some(t=>t.packing_code.toUpperCase()!==String(f.locationCode||'').trim().toUpperCase()))throw new Error('location');const serviceId=String(f.carrierServiceId||'').trim();const service=serviceId?(await c.query(`SELECT * FROM carrier_services WHERE company_id=$1 AND id=$2 AND active`,[s.companyId,serviceId])).rows[0]:null;if(serviceId&&!service)throw new Error('carrier');const shipmentNo=`SHP-${order.order_no}`,shipment=(await c.query(`INSERT INTO shipments(company_id,order_id,shipment_no,carrier,tracking_number,dispatched_by,carrier_service_id,promised_dispatch_at,expected_delivery_at,delivery_status) VALUES($1,$2,$3,$4,$5,$6,$7,date_trunc('day',now())+coalesce($8::time,'23:59'::time),CASE WHEN $9::int IS NULL THEN NULL ELSE now()+make_interval(days=>$9) END,'ready_for_pickup') RETURNING id,expected_delivery_at`,[s.companyId,id,shipmentNo,service?.name||String(f.carrier||'').trim()||null,String(f.trackingNumber||'').trim()||null,s.userId,service?.id||null,service?.cutoff_time||null,service?.transit_days??null])).rows[0];await c.query(`INSERT INTO shipment_tracking_events(company_id,shipment_id,status,note,created_by) VALUES($1,$2,'ready_for_pickup','Shipment confirmed and ready for carrier pickup.',$3)`,[s.companyId,shipment.id,s.userId]);if(order.store_id)await c.query(`INSERT INTO store_notifications(company_id,user_id,store_id,event_key,notification_type,title,message,link_path,entity_type,entity_id) SELECT $1,a.user_id,$2,$3,'approved','Order dispatched',$4,'/store-portal/deliveries','shipment',$5 FROM store_user_assignments a WHERE a.company_id=$1 AND a.store_id=$2 ON CONFLICT DO NOTHING`,[s.companyId,order.store_id,`shipment:${shipment.id}:dispatched`,`${shipmentNo} was dispatched${shipment.expected_delivery_at?` and is expected ${new Date(shipment.expected_delivery_at).toLocaleDateString()}`:''}.`,shipment.id]);for(const t of tasks)await c.query(`INSERT INTO inventory_ledger(company_id,warehouse_id,location_id,item_id,movement_type,quantity,uom,lot_number,expiry_date,reference_type,reference_id,created_by,transaction_group_id) VALUES($1,$2,$3,$4,'shipment_issue',$5,$6,$7,$8,'shipment',$9,$10,$11)`,[s.companyId,order.warehouse_id,t.to_location_id,t.item_id,-Number(t.quantity),t.uom,t.lot_number,t.expiry_date,shipment.id,s.userId,crypto.randomUUID()]);await c.query(`UPDATE sales_order_lines SET shipped_quantity=ordered_quantity WHERE company_id=$1 AND order_id=$2`,[s.companyId,id]);await c.query(`UPDATE packing_cartons SET status='dispatched' WHERE company_id=$1 AND order_id=$2 AND status='sealed'`,[s.companyId,id]);await c.query(`UPDATE sales_orders SET status='dispatched' WHERE id=$1`,[id]);return shipment.id});return Response.redirect(new URL(mobile?`/app/dispatch/mobile?dispatched=${shipmentId}`:`/app/orders/${id}?dispatched=1`,request.url),303)}catch(e){const code=e instanceof Error?e.message:'dispatch';return Response.redirect(new URL(mobile?`/app/dispatch/mobile?error=${code}`:`/app/orders/${id}?error=${code}`,request.url),303)}}
