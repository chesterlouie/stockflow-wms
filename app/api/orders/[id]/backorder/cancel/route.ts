import{z}from'zod';import{getSession}from'../../../../../../lib/auth';import{withTenant}from'../../../../../../lib/db';import{assertWarehouseAccess}from'../../../../../../lib/warehouse-access';
const schema=z.object({reason:z.string().trim().min(3).max(500)});
export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const{id}=await params,p=schema.safeParse(Object.fromEntries(await r.formData()));if(!p.success)return Response.redirect(new URL(`/app/orders/${id}?error=cancel`,r.url),303);try{await withTenant(s.companyId,async c=>{const o=(await c.query(`SELECT * FROM sales_orders WHERE company_id=$1 AND id=$2 AND parent_order_id IS NOT NULL AND status='new' AND backorder_status IN('waiting','ready') FOR UPDATE`,[s.companyId,id])).rows[0];if(!o)throw new Error('state');await assertWarehouseAccess(c,s,o.warehouse_id);await c.query(`UPDATE sales_orders SET status='cancelled',backorder_status='cancelled',backorder_cancelled_by=$1,backorder_cancelled_at=now(),backorder_cancel_reason=$2 WHERE id=$3`,[s.userId,p.data.reason,id]);await c.query(`INSERT INTO store_notifications(company_id,user_id,store_id,event_key,notification_type,title,message,entity_type,entity_id) SELECT $1,u.user_id,$2,$3,'rejected','VKIT backorder cancelled',$4,'sales_order',$5 FROM (SELECT $6::uuid user_id UNION SELECT a.user_id FROM store_user_assignments a WHERE a.company_id=$1 AND a.store_id=$2 AND a.store_role='store_manager')u WHERE $2::uuid IS NOT NULL ON CONFLICT DO NOTHING`,[s.companyId,o.store_id,`backorder:${id}:cancelled`,`${o.order_no} was cancelled. Reason: ${p.data.reason}`,id,o.created_by]);await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id,details) VALUES($1,$2,'vkit_backorder_cancelled','sales_order',$3,$4::jsonb)`,[s.companyId,s.userId,id,JSON.stringify({reason:p.data.reason,parentOrderId:o.parent_order_id})])});return Response.redirect(new URL('/app/orders',r.url),303)}catch{return Response.redirect(new URL(`/app/orders/${id}?error=cancel`,r.url),303)}}
