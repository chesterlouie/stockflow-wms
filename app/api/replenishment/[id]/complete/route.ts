import {getSession} from '../../../../../lib/auth';import {withTenant} from '../../../../../lib/db';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);const {id}=await params;const f=Object.fromEntries(await request.formData());try{await withTenant(s.companyId,async c=>{const t=(await c.query(`SELECT t.*,f.code AS from_code,d.code AS to_code,f.warehouse_id FROM replenishment_tasks t JOIN locations f ON f.id=t.from_location_id JOIN locations d ON d.id=t.to_location_id WHERE t.company_id=$1 AND t.id=$2 AND t.status='pending' FOR UPDATE`,[s.companyId,id])).rows[0];if(!t||String(f.locationCode||'').toUpperCase()!==t.from_code||String(f.destinationCode||'').toUpperCase()!==t.to_code)throw new Error('scan');if(!(await c.query('SELECT 1 FROM item_barcodes WHERE company_id=$1 AND item_id=$2 AND barcode_value=$3',[s.companyId,t.item_id,String(f.barcode||'')])).rowCount)throw new Error('barcode');const available=Number((await c.query(`SELECT COALESCE(sum(quantity),0) AS q FROM inventory_ledger WHERE company_id=$1 AND item_id=$2 AND location_id=$3 AND lot_number IS NOT DISTINCT FROM $4 AND expiry_date IS NOT DISTINCT FROM $5::date`,[s.companyId,t.item_id,t.from_location_id,t.lot_number,t.expiry_date])).rows[0].q);if(available<Number(t.quantity))throw new Error('stock');const g=crypto.randomUUID();await c.query(`INSERT INTO inventory_ledger(company_id,warehouse_id,location_id,item_id,movement_type,quantity,uom,lot_number,expiry_date,reference_type,reference_id,created_by,transaction_group_id) VALUES($1,$2,$3,$4,'replenish_out',$5,$6,$7,$8,'replenishment',$9,$10,$11),($1,$2,$12,$4,'replenish_in',$13,$6,$7,$8,'replenishment',$9,$10,$11)`,[s.companyId,t.warehouse_id,t.from_location_id,t.item_id,-Number(t.quantity),t.uom,t.lot_number,t.expiry_date,id,s.userId,g,t.to_location_id,Number(t.quantity)]);await c.query(`UPDATE replenishment_tasks SET status='completed',completed_at=now(),completed_by=$1 WHERE id=$2`,[s.userId,id])});return Response.redirect(new URL('/app/replenishment?completed=1',request.url),303)}catch(error){const code=error instanceof Error?error.message:'failed';return Response.redirect(new URL(`/app/replenishment?error=${code}`,request.url),303)}}
