import{getSession}from'../../../../../lib/auth';import{withTenant}from'../../../../../lib/db';export async function POST(r:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);const f=await r.formData(),id=String(f.get('id')||'');await withTenant(s.companyId,c=>id?c.query(`UPDATE carrier_sla_notifications SET read_at=coalesce(read_at,now()) WHERE company_id=$1 AND user_id=$2 AND id=$3`,[s.companyId,s.userId,id]):c.query(`UPDATE carrier_sla_notifications SET read_at=coalesce(read_at,now()) WHERE company_id=$1 AND user_id=$2`,[s.companyId,s.userId]));return Response.redirect(new URL('/app/carrier-scorecards/inbox',r.url),303)}
