import{z}from'zod';import{getSession}from'../../../../lib/auth';import{withTenant}from'../../../../lib/db';const schema=z.object({enabled:z.preprocess(v=>v==='on',z.boolean()),reviewDay:z.coerce.number().int().min(1).max(28),contractWarningDays:z.coerce.number().int().min(1).max(365),correctiveActionGraceHours:z.coerce.number().int().min(0).max(720)});export async function POST(r:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);if(!['owner','admin'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await r.formData()));if(!p.success)return Response.redirect(new URL('/app/carrier-scorecards/governance?error=config',r.url),303);await withTenant(s.companyId,async c=>{await c.query(`INSERT INTO carrier_sla_automation_configs(company_id,enabled,review_day,contract_warning_days,corrective_action_grace_hours,updated_by) VALUES($1,$2,$3,$4,$5,$6) ON CONFLICT(company_id) DO UPDATE SET enabled=excluded.enabled,review_day=excluded.review_day,contract_warning_days=excluded.contract_warning_days,corrective_action_grace_hours=excluded.corrective_action_grace_hours,updated_by=excluded.updated_by,updated_at=now()`,[s.companyId,p.data.enabled,p.data.reviewDay,p.data.contractWarningDays,p.data.correctiveActionGraceHours,s.userId]);await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id,details) VALUES($1,$2,'carrier_sla_automation_configured','company',$1,$3::jsonb)`,[s.companyId,s.userId,JSON.stringify(p.data)])});return Response.redirect(new URL('/app/carrier-scorecards/governance?configured=1',r.url),303)}
