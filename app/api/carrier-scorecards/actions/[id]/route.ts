import{z}from'zod';import{getSession}from'../../../../../lib/auth';import{withTenant}from'../../../../../lib/db';import{assertWarehouseAccess}from'../../../../../lib/warehouse-access';const schema=z.object({status:z.enum(['in_progress','completed','cancelled']),note:z.string().trim().min(3).max(800)});export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const{id}=await params,p=schema.safeParse(Object.fromEntries(await r.formData()));if(!p.success)return Response.redirect(new URL('/app/carrier-scorecards?error=corrective',r.url),303);try{await withTenant(s.companyId,async c=>{const a=(await c.query(`SELECT a.*,r.warehouse_id FROM carrier_corrective_actions a JOIN carrier_performance_reviews r ON r.id=a.review_id WHERE a.company_id=$1 AND a.id=$2 AND a.status IN('open','in_progress') FOR UPDATE OF a`,[s.companyId,id])).rows[0];if(!a)throw new Error('state');await assertWarehouseAccess(c,s,a.warehouse_id);if(s.role==='manager'&&a.owner_id!==s.userId)throw new Error('owner');await c.query(`UPDATE carrier_corrective_actions SET status=$1,completion_note=$2,completed_at=CASE WHEN $1 IN('completed','cancelled') THEN now() END,completed_by=CASE WHEN $1 IN('completed','cancelled') THEN $3 END WHERE id=$4`,[p.data.status,p.data.note,s.userId,id]);await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id,details) VALUES($1,$2,'carrier_corrective_action_updated','carrier_corrective_action',$3,$4::jsonb)`,[s.companyId,s.userId,id,JSON.stringify(p.data)])});return Response.redirect(new URL('/app/carrier-scorecards?actionUpdated=1',r.url),303)}catch(e){return Response.redirect(new URL(`/app/carrier-scorecards?error=${e instanceof Error?e.message:'corrective'}`,r.url),303)}}
