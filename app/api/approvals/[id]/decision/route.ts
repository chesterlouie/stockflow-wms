import{z}from'zod';import{getSession}from'../../../../../lib/auth';import{withTenant}from'../../../../../lib/db';const schema=z.object({decision:z.enum(['approved','rejected','returned']),comment:z.string().trim().max(500).optional()});export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);const{id}=await params,p=schema.safeParse(Object.fromEntries(await r.formData()));if(!p.success)return Response.redirect(new URL('/app/approvals?error=decision',r.url),303);try{await withTenant(s.companyId,async c=>{const q=(await c.query(`SELECT q.*,r.required_steps,rs.approver_role FROM approval_requests q JOIN approval_rules r ON r.id=q.rule_id JOIN approval_rule_steps rs ON rs.rule_id=r.id AND rs.step_no=q.current_step WHERE q.company_id=$1 AND q.id=$2 AND q.status='pending' FOR UPDATE`,[s.companyId,id])).rows[0];if(!q||q.approver_role!==s.role)throw new Error('role');if(p.data.decision!=='approved'&&!p.data.comment)throw new Error('comment');await c.query(`INSERT INTO approval_decisions(company_id,request_id,step_no,decision,comment,decided_by) VALUES($1,$2,$3,$4,$5,$6)`,[s.companyId,id,q.current_step,p.data.decision,p.data.comment||null,s.userId]);if(p.data.decision!=='approved'){await c.query(`UPDATE approval_requests SET status=$1,completed_at=now() WHERE id=$2`,[p.data.decision,id]);return}if(q.current_step<q.required_steps){await c.query(`UPDATE approval_requests SET current_step=current_step+1 WHERE id=$1`,[id]);return}const x=typeof q.payload==='string'?JSON.parse(q.payload):q.payload;if(q.operation_type==='forecast_replenishment'){const f=(await c.query(`SELECT rr.*,i.base_uom,b.lot_number,b.expiry_date FROM replenishment_recommendations rr JOIN items i ON i.id=rr.item_id LEFT JOIN inventory_balances b ON b.company_id=rr.company_id AND b.item_id=rr.item_id AND b.location_id=rr.from_location_id WHERE rr.company_id=$1 AND rr.id=$2 AND rr.status='pending' FOR UPDATE`,[s.companyId,q.entity_id])).rows[0];if(!f)throw new Error('state');const task=(await c.query(`INSERT INTO replenishment_tasks(company_id,item_id,from_location_id,to_location_id,quantity,uom,lot_number,expiry_date) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING id`,[s.companyId,f.item_id,f.from_location_id,f.to_location_id,f.recommended_quantity,f.base_uom,f.lot_number,f.expiry_date])).rows[0];await c.query(`UPDATE replenishment_recommendations SET status='converted',reviewed_at=now(),reviewed_by=$1,replenishment_task_id=$2 WHERE id=$3`,[s.userId,task.id,q.entity_id])}else if(q.operation_type==='inventory_adjustment'){const resource=(await c.query(`SELECT i.base_uom,l.warehouse_id FROM items i CROSS JOIN locations l WHERE i.company_id=$1 AND l.company_id=$1 AND i.id=$2 AND l.id=$3`,[s.companyId,x.itemId,x.locationId])).rows[0];if(!resource)throw new Error('state');const current=Number((await c.query(`SELECT coalesce(sum(quantity),0) q FROM inventory_ledger WHERE company_id=$1 AND item_id=$2 AND location_id=$3 AND lot_number IS NOT DISTINCT FROM $4 AND expiry_date IS NOT DISTINCT FROM $5::date`,[s.companyId,x.itemId,x.locationId,x.lotNumber||null,x.expiryDate||null])).rows[0].q);if(Number(x.quantity)<0&&current<Math.abs(Number(x.quantity)))throw new Error('stock');await c.query(`INSERT INTO inventory_ledger(company_id,warehouse_id,location_id,item_id,movement_type,quantity,uom,lot_number,expiry_date,reference_type,reference_id,created_by,transaction_group_id,reason_code,note) VALUES($1,$2,$3,$4,'adjustment',$5,$6,$7,$8,'adjustment',$9,$10,$11,$12,$13)`,[s.companyId,resource.warehouse_id,x.locationId,x.itemId,x.quantity,x.uom,x.lotNumber||null,x.expiryDate||null,x.referenceId,s.userId,crypto.randomUUID(),x.reasonCode,x.note||null])}await c.query(`UPDATE approval_requests SET status='executed',completed_at=now() WHERE id=$1`,[id]);await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id) VALUES($1,$2,'approval_executed','approval_request',$3)`,[s.companyId,s.userId,id])});return Response.redirect(new URL('/app/approvals?decided=1',r.url),303)}catch{return Response.redirect(new URL('/app/approvals?error=decision',r.url),303)}}
