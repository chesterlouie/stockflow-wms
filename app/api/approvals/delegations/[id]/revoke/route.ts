import {getSession} from '../../../../../../lib/auth';import {withTenant} from '../../../../../../lib/db';
export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);const {id}=await params;await withTenant(s.companyId,async c=>{await c.query(`UPDATE approval_delegations SET active=false WHERE company_id=$1 AND id=$2 AND (delegator_id=$3 OR $4 IN('owner','admin'))`,[s.companyId,id,s.userId,s.role]);await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id) VALUES($1,$2,'approval_delegation_revoked','approval_delegation',$3)`,[s.companyId,s.userId,id])});return Response.redirect(new URL('/app/approvals?delegated=1',r.url),303)}
