import { getSession } from "../../../../../lib/auth";import { withTenant } from "../../../../../lib/db";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);const {id}=await params;try{const receiptId=await withTenant(s.companyId,async c=>{const t=(await c.query(`SELECT p.*,r.id AS receipt_id,l.warehouse_id,rl.expected_quantity,rl.accepted_quantity,rl.held_quantity,rl.damaged_quantity FROM putaway_tasks p JOIN inbound_receipt_lines rl ON rl.id=p.receipt_line_id JOIN inbound_receipts r ON r.id=rl.receipt_id JOIN locations l ON l.company_id=p.company_id AND l.id=p.from_location_id WHERE p.company_id=$1 AND p.id=$2 AND p.status='pending' FOR UPDATE`,[s.companyId,id])).rows[0];if(!t)throw new Error();const available=(await c.query(`SELECT coalesce(sum(quantity),0) AS q FROM inventory_ledger WHERE company_id=$1 AND item_id=$2 AND location_id=$3 AND lot_number IS NOT DISTINCT FROM $4 AND expiry_date IS NOT DISTINCT FROM $5::date`,[s.companyId,t.item_id,t.from_location_id,t.lot_number,t.expiry_date])).rows[0].q;if(Number(available)<Number(t.quantity))throw new Error();const g=crypto.randomUUID();await c.query(`INSERT INTO inventory_ledger(company_id,warehouse_id,location_id,item_id,movement_type,quantity,uom,lot_number,expiry_date,reference_type,reference_id,created_by,transaction_group_id) VALUES($1,$2,$3,$4,'putaway_out',$5,$6,$7,$8,'putaway',$9,$10,$11),($1,$2,$12,$4,'putaway_in',$13,$6,$7,$8,'putaway',$9,$10,$11)`,[s.companyId,t.warehouse_id,t.from_location_id,t.item_id,-Number(t.quantity),t.uom,t.lot_number,t.expiry_date,id,s.userId,g,t.to_location_id,Number(t.quantity)]);await c.query(`UPDATE putaway_tasks SET status='completed',completed_at=now(),completed_by=$1 WHERE id=$2`,[s.userId,id]);const pending=Number((await c.query(`SELECT count(*) AS count FROM putaway_tasks WHERE company_id=$1 AND receipt_line_id=$2 AND status='pending'`,[s.companyId,t.receipt_line_id])).rows[0].count);const received=Number(t.accepted_quantity)+Number(t.held_quantity)+Number(t.damaged_quantity);const status=received<Number(t.expected_quantity)?'partial':pending?'putaway':'completed';await c.query(`UPDATE inbound_receipt_lines SET status=$1 WHERE id=$2`,[status,t.receipt_line_id]);await c.query(`UPDATE inbound_receipts SET status=$1 WHERE id=$2`,[status,t.receipt_id]);return t.receipt_id});return Response.redirect(new URL(`/app/receiving/${receiptId}?putaway=1`,request.url),303)}catch{return Response.redirect(new URL('/app/receiving?error=putaway',request.url),303)}}
