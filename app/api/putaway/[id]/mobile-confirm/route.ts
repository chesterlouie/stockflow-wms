import {getSession} from '../../../../../lib/auth';import {withTenant} from '../../../../../lib/db';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);const {id}=await params;const form=await request.formData();const valid=await withTenant(s.companyId,async c=>(await c.query(`SELECT 1 FROM putaway_tasks t JOIN locations f ON f.id=t.from_location_id JOIN locations d ON d.id=t.to_location_id JOIN item_barcodes b ON b.item_id=t.item_id AND b.company_id=t.company_id WHERE t.company_id=$1 AND t.id=$2 AND t.status='pending' AND f.code=$3 AND d.code=$4 AND b.barcode_value=$5`,[s.companyId,id,String(form.get('locationCode')||'').toUpperCase(),String(form.get('destinationCode')||'').toUpperCase(),String(form.get('barcode')||'')])).rowCount);if(!valid)return Response.redirect(new URL('/app/putaway/mobile?error=scan',request.url),303);return Response.redirect(new URL(`/api/putaway/${id}/complete`,request.url),307)}
