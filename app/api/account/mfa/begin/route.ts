import {getSession} from '../../../../../lib/auth';import {db} from '../../../../../lib/db';import {encryptSecret,newTotpSecret} from '../../../../../lib/mfa';
export async function POST(request:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);await db().query(`INSERT INTO user_mfa(user_id,secret_encrypted,enabled) VALUES($1,$2,false) ON CONFLICT(user_id) DO UPDATE SET secret_encrypted=$2,enabled=false,confirmed_at=NULL,updated_at=now()`,[s.userId,await encryptSecret(newTotpSecret())]);return Response.redirect(new URL('/account/security',request.url),303)}
