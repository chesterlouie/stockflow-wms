import { hash } from "bcryptjs";import { getSession } from "../../../../../lib/auth";import { withTenant } from "../../../../../lib/db";
function temporaryPassword(){const b=crypto.getRandomValues(new Uint8Array(12));return `Sf!${Array.from(b,x=>x.toString(16).padStart(2,'0')).join('').slice(0,18)}aA1`}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin'].includes(s.role))return new Response('Forbidden',{status:403});const {id}=await params;const password=temporaryPassword();const email=await withTenant(s.companyId,async c=>{const member=(await c.query(`SELECT u.email FROM company_members m JOIN users u ON u.id=m.user_id WHERE m.company_id=$1 AND m.user_id=$2 AND m.role<>'owner'`,[s.companyId,id])).rows[0];if(!member)throw new Error();await c.query(`UPDATE users SET password_hash=$1,must_change_password=true WHERE id=$2`,[await hash(password,12),id]);await c.query(`DELETE FROM auth_sessions WHERE user_id=$1`,[id]);await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id) VALUES($1,$2,'password_reset','user',$3)`,[s.companyId,s.userId,id]);return member.email});return new Response(`<!doctype html><html><head><meta charset="utf-8"><title>Password reset</title><style>body{font-family:system-ui;background:#f4f7f5;padding:40px;color:#14231e}.card{max-width:700px;margin:auto;background:#fff;border:1px solid #dbe5e1;border-radius:12px;padding:28px}code{display:block;background:#e9f3ef;padding:16px;margin:16px 0}a{color:#087f5b;font-weight:700}</style></head><body><main class="card"><h1>Temporary password created</h1><p>Share this securely with <strong>${email}</strong>.</p><code>${password}</code><p>Existing sessions were revoked.</p><a href="/app/users">Return to users</a></main></body></html>`,{headers:{'content-type':'text/html; charset=utf-8'}})}
