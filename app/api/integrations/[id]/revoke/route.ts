import { getSession } from "../../../../../lib/auth";import { withTenant } from "../../../../../lib/db";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin'].includes(s.role))return new Response('Forbidden',{status:403});const {id}=await params;await withTenant(s.companyId,c=>c.query(`UPDATE api_clients SET active=false WHERE company_id=$1 AND id=$2`,[s.companyId,id]));return Response.redirect(new URL('/app/integrations?revoked=1',request.url),303)}
