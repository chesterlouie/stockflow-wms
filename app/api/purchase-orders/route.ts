import {getSession} from '../../../lib/auth';import {withTenant} from '../../../lib/db';import {z} from 'zod';const schema=z.object({poNo:z.string().trim().min(1).max(50),supplierId:z.string().uuid(),warehouseId:z.string().uuid(),expectedDate:z.string().optional()});
export async function POST(request:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await request.formData()));if(!p.success)return Response.redirect(new URL('/app/purchasing?error=po',request.url),303);try{const id=await withTenant(s.companyId,async c=>(await c.query(`INSERT INTO purchase_orders(company_id,warehouse_id,supplier_id,po_no,expected_date,created_by) SELECT $1,w.id,s.id,$4,$5,$6 FROM warehouses w JOIN suppliers s ON s.company_id=w.company_id WHERE w.company_id=$1 AND w.id=$2 AND s.id=$3 AND s.status='active' RETURNING purchase_orders.id`,[s.companyId,p.data.warehouseId,p.data.supplierId,p.data.poNo,p.data.expectedDate||null,s.userId])).rows[0]?.id);if(!id)throw new Error();return Response.redirect(new URL(`/app/purchasing/${id}`,request.url),303)}catch{return Response.redirect(new URL('/app/purchasing?error=duplicate',request.url),303)}}
