import {getSession} from '../../../../../lib/auth';import {withTenant} from '../../../../../lib/db';import {z} from 'zod';const schema=z.object({capacity:z.union([z.literal(''),z.coerce.number().positive().max(999999999)])});
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const {id}=await params;const p=schema.safeParse(Object.fromEntries(await request.formData()));if(!p.success)return Response.redirect(new URL('/app/replenishment?error=capacity',request.url),303);await withTenant(s.companyId,c=>c.query('UPDATE locations SET capacity_quantity=$1 WHERE company_id=$2 AND id=$3',[p.data.capacity===''?null:p.data.capacity,s.companyId,id]));return Response.redirect(new URL('/app/replenishment?saved=capacity',request.url),303)}
