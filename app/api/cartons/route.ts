import {z} from 'zod';import {getSession} from '../../../lib/auth';import {withTenant} from '../../../lib/db';
const schema=z.object({orderId:z.string().uuid(),cartonNo:z.string().trim().min(1).max(50)});
export async function POST(request:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',request.url),303);if(!['owner','admin','manager','operator'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await request.formData()));if(!p.success)return Response.redirect(new URL('/app/packing/cartons?error=invalid',request.url),303);try{await withTenant(s.companyId,c=>c.query(`INSERT INTO packing_cartons(company_id,order_id,carton_no,created_by) SELECT $1,id,$3,$4 FROM sales_orders WHERE company_id=$1 AND id=$2 AND status IN('allocated','picking','picked','packing','packed')`,[s.companyId,p.data.orderId,p.data.cartonNo,s.userId]));return Response.redirect(new URL('/app/packing/cartons?created=1',request.url),303)}catch{return Response.redirect(new URL('/app/packing/cartons?error=duplicate',request.url),303)}}
