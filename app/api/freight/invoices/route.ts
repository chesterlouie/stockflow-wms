import{z}from'zod';import{getSession}from'../../../../lib/auth';import{withTenant}from'../../../../lib/db';
const schema=z.object({carrierServiceId:z.string().uuid(),invoiceNumber:z.string().trim().min(1).max(80),invoiceDate:z.string().date(),dueDate:z.preprocess(v=>v===''?undefined:v,z.string().date().optional()),currency:z.string().trim().length(3),totalAmount:z.coerce.number().min(0),source:z.enum(['manual','csv','api']).default('manual'),sourceReference:z.string().trim().max(120).optional(),notes:z.string().trim().max(500).optional()});
export async function POST(r:Request){const s=await getSession();if(!s)return Response.redirect(new URL('/signin',r.url),303);if(!['owner','admin','manager'].includes(s.role))return new Response('Forbidden',{status:403});const p=schema.safeParse(Object.fromEntries(await r.formData()));if(!p.success)return Response.redirect(new URL('/app/freight/reconciliation?error=invoice',r.url),303);try{await withTenant(s.companyId,async c=>{const saved=await c.query(`INSERT INTO carrier_freight_invoices(company_id,carrier_service_id,invoice_number,invoice_date,due_date,currency,total_amount,source,source_reference,notes,created_by) SELECT $1,cs.id,$3,$4,$5,$6,$7,$8,$9,$10,$11 FROM carrier_services cs WHERE cs.company_id=$1 AND cs.id=$2 AND cs.active RETURNING id`,[s.companyId,p.data.carrierServiceId,p.data.invoiceNumber,p.data.invoiceDate,p.data.dueDate||null,p.data.currency.toUpperCase(),p.data.totalAmount,p.data.source,p.data.sourceReference||null,p.data.notes||null,s.userId]);if(!saved.rowCount)throw new Error('service');await c.query(`INSERT INTO audit_logs(company_id,user_id,action,entity_type,entity_id,details) VALUES($1,$2,'carrier_invoice_created','carrier_freight_invoice',$3,$4::jsonb)`,[s.companyId,s.userId,saved.rows[0].id,JSON.stringify({invoiceNumber:p.data.invoiceNumber,totalAmount:p.data.totalAmount,source:p.data.source})])});return Response.redirect(new URL('/app/freight/reconciliation?created=1',r.url),303)}catch(e){return Response.redirect(new URL(`/app/freight/reconciliation?error=${e instanceof Error?e.message:'invoice'}`,r.url),303)}}
