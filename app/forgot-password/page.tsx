import Link from 'next/link';
export const metadata={title:'Forgot password'};
export default async function Forgot({searchParams}:{searchParams:Promise<{sent?:string}>}){const q=await searchParams;return <main className="auth-page"><section className="auth-brand-panel"><Link href="/" className="brand"><span className="brand-mark">W</span><span>Warevanta</span></Link><div className="auth-message"><h1>Recover your account securely.</h1><p>We will send a one-time reset link that expires after one hour.</p></div><small>Secure warehouse access</small></section><section className="auth-form-side"><div className="auth-card"><h2>Reset your password</h2>{q.sent?<div className="success-banner">If that email belongs to an account, a reset link has been sent.</div>:<form className="form-stack" method="post" action="/api/auth/forgot-password"><div className="field"><label>Work email</label><input name="email" type="email" required/></div><button className="button button-primary">Send reset link</button></form>}<Link href="/signin">Return to sign in</Link></div></section></main>}
